# TODO

- **Ban endpoint**: `POST /api/sessions/:code/bans` to ban users by user ID.
- **Ban UI**: Add ban button per question in admin view.
- **HttpOnly cookies**: Move auth token from localStorage to HttpOnly cookies for XSS protection.
- **Pagination**: Add LIMIT/OFFSET to question list API.
