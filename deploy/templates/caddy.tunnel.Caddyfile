__PUBLIC_HOST__ {
    encode zstd gzip

    # API from local machine via SSH reverse tunnel
    reverse_proxy /api/* 127.0.0.1:__REMOTE_BACKEND_INTERNAL_PORT__

    # Frontend from local machine via SSH reverse tunnel
    reverse_proxy 127.0.0.1:__REMOTE_FRONTEND_INTERNAL_PORT__
}
